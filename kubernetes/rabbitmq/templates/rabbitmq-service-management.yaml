apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-service-management
  labels:
    app: rabbitmq
spec:
  selector:
    app: rabbitmq 
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 15672
      targetPort: 15672
